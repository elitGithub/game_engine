{
  "name": "project-velocity-canvas",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/project-velocity-canvas/src",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "options": {
        "outputPath": "dist/apps/project-velocity-canvas"
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "project-velocity-canvas:build"
      }
    },
    "check:types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/project-velocity-canvas/tsconfig.json"
      }
    }
  }
}
