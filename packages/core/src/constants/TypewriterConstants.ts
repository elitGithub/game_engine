/**
 * TypewriterConstants - Default configuration values for TypewriterEffect
 *
 * These are sensible defaults for typewriter animation effects.
 * All values can be overridden via TypewriterEffect constructor configuration.
 */

/**
 * Default text reveal speed in characters per second.
 * Common typewriter speeds: 20-60 cps.
 */
export const DEFAULT_TYPEWRITER_SPEED = 30;

/**
 * Milliseconds per second constant for time conversion.
 */
export const MILLISECONDS_PER_SECOND = 1000;
