{
  "name": "@game-engine/workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "build:core": "nx build core",
    "test": "nx run-many -t test",
    "test:core": "nx test core",
    "test:ui": "nx test core --ui",
    "check:types": "nx run-many -t check:types",
    "check:types:core": "nx check:types core",
    "dev": "nx dev core",
    "preview": "nx preview core",
    "nx": "nx"
  },
  "dependencies": {
    "semver": "^7.7.3"
  },
  "devDependencies": {
    "@nx/js": "22.0.3",
    "@nx/vite": "22.0.3",
    "@types/node": "^24.10.0",
    "@types/semver": "^7.7.1",
    "@vitest/ui": "^2.0.4",
    "jsdom": "^25.0.1",
    "nx": "22.0.3",
    "typescript": "^5.9.3",
    "vite": "^5.0.0",
    "vitest": "^2.0.4"
  }
}
